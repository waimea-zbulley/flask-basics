# Flask Minimal Docker Container

This is a minimal Docker container for a [Flask web app](https://flask.palletsprojects.com/), built using [Python](https://www.python.org/).

- Run Docker Desktop / Rancher Desktop
- Open a terminal window to the site folder
- Run `docker compose up --build`
- Browse to `localhost:5000`

